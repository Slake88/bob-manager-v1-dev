name: Temporary event refresh

on:
  push:
    branches:
      - rc1-rebuild

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check workflow
        run: echo ready
